{
  "name": "hwp-agent-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android",
    "android:debug": "cd android && ./gradlew assembleDebug",
    "android:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
